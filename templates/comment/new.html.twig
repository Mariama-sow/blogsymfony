{% extends 'base.html.twig' %}

{% block title %}Commenter l'article{% endblock %}

{% block body %}
    <h2>Ajouter un commentaire pour : {{ article.title }}</h2>

    {{ form_start(form) }}
        {{ form_row(form.content) }}
        <button class="btn btn-primary">Envoyer</button>
    {{ form_end(form) }}
{% endblock %}
